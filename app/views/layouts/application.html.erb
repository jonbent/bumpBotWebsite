<!DOCTYPE html>
<html>
  <head>
    <title>Website</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">bumpBot</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarText" aria-controls="navbarText" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarText">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item active">
          <% if logged_in? %>
            
          <% end %>
        </li>
      </ul>
      <span class="navbar-text">
        <% if logged_in? %>
          <a href="/logout">
            <span class="nav-link">
              <img src="https://cdn.discordapp.com/avatars/<%= current_user['_id'].to_s + '/' + current_user['avatar'].to_s %>?size=30x30" class="rounded-circle size-30" /> <span class="nav-name"><%= session[:user]["username"] %></span><span class="nav-discriminator">#<%=session[:user]["discriminator"]%></span>
            </span>
          </a>
        <% else %>
          <a href="/auth">Login with Discord</a>
        <% end %>
      </span>
    </div>
  </nav>
    <%= yield %>
  </body>
</html>
